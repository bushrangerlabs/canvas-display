# Canvas Routines acceptance evidence — 2026-08-02

Status: **functional and hardware acceptance passed; production security approval pending**

Command executed:

```text
bash scripts/acceptance-routines.sh --live-host <redacted> --restart-check
```

Passed evidence:

- routine contract validation;
- Core TypeScript type-check;
- 416 Core tests with zero failures;
- production web build;
- live Core health before and after application-container restart;
- identical authoritative row counts before and after restart;
- enrolled Pi Edge Agent reconnected;
- Pi kiosk compatibility channel reconnected;
- device credential/invitation counts and a non-empty one-way enrollment-seed fingerprint remained
  unchanged across restart; no seed value was emitted;
- no recent routine startup or migration error matched the acceptance filter.
- production open pairing is disabled, the independently configured security epoch is 1, and the
  restart gate proved that epoch remains unchanged while the enrolled Pi reconnects;
- Core tests prove a correctly signed credential issued at one epoch is rejected when Core expects
  a newer epoch. This validates the stale-credential fence without invalidating the live Pi.
- the clean-room failure/rollback case passes: a known-good revision succeeds, a deliberately broken
  immutable revision fails, the routine is disabled, revision 1 is reactivated, all three execution
  records remain present, and the restored revision succeeds again.
- a custom-format live PostgreSQL backup restored into an isolated temporary database;
- sampled authoritative table inventories matched between source and restored databases;
- restored routine revision/execution references had no orphan rows;
- credential/invitation inventories restored and credential-to-device references had no orphan rows;
- a synthetic enabled routine, immutable revision, successful execution, step result, and learned
  fast-path record were created only in the first temporary restore;
- a second backup/restore preserved the non-empty content digest and active-revision linkage;
- Core now mounts the persistent `canvas-core-assets` volume at `/app/data/assets`;
- live asset metadata/object size/hash verification passed (the current inventory is empty);
- a synthetic content-addressed object was archived between disposable volumes and restored with
  identical size and SHA-256;
- the temporary restore database and dump were removed after verification.
- strict validation accepted the physical report
  `canvas-pi-acceptance-device-2acc4690-b00b-4bf7-9ffe-a532ddf93150-2026-08-02T10-17-22-868Z.json`;
- all nine device-scoped hardware observations passed: inventory (3 microphones, 3 speakers,
  6 wake-word models), Pi speaker, microphone loopback, `hey_jarvis` detection, wake/good/no-intent
  cues, the complete voice/TTS loop without clipping or early relistening, and the silent/no-intent loop;
- live binding verified that the evidence device is paired, `connected`, unrevoked, and represented by
  recent exact-identity Edge Agent and kiosk connections;
- the live acceptance gate passed after correcting its connection-event window and canonical
  `connected` status check.

Post-restart live inventory was `devices=2`, `scenes=5`, and `settings=22`; live routine tables were
empty. Non-empty routine-history backup/restore was instead proven entirely inside disposable
clean-room databases so acceptance data did not pollute production.

Deferred operational/security work:

- repeat restart evidence with reviewed non-empty routine/revision/execution/learning data;
- perform the destructive epoch-increment and controlled Pi re-enrolment drill only in an approved
  maintenance window. It is not required for normal audio, voice, or routine operation; automated
  tests already cover stale-credential rejection and canonical device-ID preservation;
- named human security approval of the collected evidence.

Functional release acceptance is complete. Phase 7 remains gated pending the explicitly separate
production security approval and deferred disaster-recovery evidence.
